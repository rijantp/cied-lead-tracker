import { GraphBarDirective } from './graph-bar.directive';

describe('GraphBarDirective', () => {
  it('should create an instance', () => {
    const directive = new GraphBarDirective();
    expect(directive).toBeTruthy();
  });
});
