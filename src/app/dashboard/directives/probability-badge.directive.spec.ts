import { ProbabilityBadgeDirective } from './probability-badge.directive';

describe('ProbabilityBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new ProbabilityBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
