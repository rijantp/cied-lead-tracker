import { StageTypeCountPipe } from './stage-type-count.pipe';

describe('StageTypeCountPipe', () => {
  it('create an instance', () => {
    const pipe = new StageTypeCountPipe();
    expect(pipe).toBeTruthy();
  });
});
